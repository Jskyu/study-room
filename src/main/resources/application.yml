server:
  port: 8080

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/ond
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: 1234
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: false
    hibernate:
      ddl-auto: create